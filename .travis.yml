language: node_js
node_js:
  - lts/*

before_install:
  - npm install -g hexo-cli

install:
  - npm install
  - npm install hexo-deployer-git --save
  
script:
  - npm run clean && npm run build && npm run deploy 
  